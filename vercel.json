{
    "version": 2,
    "builds": [{
      "src": "app.js",
      "use": "@vercel/node"
    }],
    "routes": [{
      "src": "/api/(.*)",
      "dest": "/api"
    }],
    "env": {
      "MONGO_URI": "@mongo_uri"
    },
    "build": {
      "env": {
        "MONGO_URI": "@mongo_uri"
      }
    }
  }